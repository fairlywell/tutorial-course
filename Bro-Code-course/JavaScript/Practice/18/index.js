/* = gán biến cho giá trị 
== so sánh giá trị 
=== so sánh giá trị và kiểu dữ liệu datatype
!= <> == so sánh giá trị ko bằng nhau ko
!== <> === so sánh giá trị và kiểu dữ liệu ko bằng nhau
*/
const PI = 3.14;

if(PI !== 3.14) {
  console.log('That is NOT Pi');
  
} else {
  console.log("that is Pi");
  
}